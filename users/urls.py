from django.urls import path
#from . import views
# from rest_framework import routers # Example for later if using ViewSets

app_name = 'users'

urlpatterns = [
    # path('register/', views.register, name='register'), # Comment out or delete
    # API endpoints for user registration, login, etc., will go here
    # Example (will implement later): path('api/register/', UserRegistrationAPIView.as_view(), name='api_register'),
]